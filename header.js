//Import Liabraries for component
import React from 'react';
import { Text } from 'react-native';

// Make the component
const Header = () => {
  return <Text>Albums!</Text>;
};

// Make the component available to other parts of App

export default Header;
